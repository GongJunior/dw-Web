1. build sqlite3 diceware database *complete*
2. design main page, push data client side
    basics
        dropdown for both list name & number of words *complete*
        generate tabled based on input *complete*
        generate page with list content if requrested
    support
        About section: Tells about the page & project, liks source & my info
        Help: Info about diceware
    advanced
        allow user to import their own list
        link to desktop client

circle back
1. caching data from database
2. keeping track of when db changes
3. The script from “http://127.0.0.1:5000/static/base.js” was loaded even though its MIME type (“text/plain”) is not a valid JavaScript MIME type. (https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/X-Content-Type-Options)
4. Check if localstorage & sessionstorage is available, if not come up with backup plan
5. Check if list has been updated, if so clear localstorage

From Bootstrap:
    dropdown